server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass https://backendleanlearn-production.up.railway.app;
        proxy_http_version 1.1;
    }

    # Serve static files
    location / {
        try_files $uri $uri/ /index.html;
    }
}
